<div class="form center">
{{ form_start(form) }}
    {{ form_widget(form) }}
</div>
    <button class="btn btn-success">{{ button_label|default('Сохранить') }}</button>
{{ form_end(form) }}

<style>
    .form{
        width: 300px;
    }
</style>